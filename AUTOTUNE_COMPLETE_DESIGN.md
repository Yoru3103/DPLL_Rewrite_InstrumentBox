# 完整 Autotune 设计与实现

> 2026-09-20：本次直接将已有指标接入完整 PS 整定；本文优先于历史分阶段计划。
> 代码和离线测试完成不代表已上板标定。原先节点 1e4469a 已上传；仅提交 Autotune 文件，其他工作区资料不上传。

## 1. 运行合同

新整定要求 AD030001 参数接口及 AD050001 指标能力，缺失返回 UNSUPPORTED；不退回不完整瞬时采样。四组现有 Kp/Ki 有界候选继承 ORIGINAL 的 Kii/Kd/D_COEF。全局互斥，整定期间拒绝改变输入、PID、中心频率、门限、限幅或长阻塞命令。

实际搜索为 ORIGINAL 加四组固定候选的有界选优，不宣称连续搜索全部PID参数空间。候选值沿用本项目已有参数范围；Kii/Kd/D_COEF保持启动时读回值，避免未经系统辨识扩大搜索。所有指标用于判定候选的安全性、性能或数据可信度，并不意味着每项指标必须对应一个独立调参旋钮。

| 候选 | 测频 Kp / Ki（十六进制 raw） | 锁相 Kp / Ki（十进制 raw） |
|---|---|---|
| 1 | 0x200000 / 0x040000 | 550000 / 250000 |
| 2 | 0x200000 / 0x080000 | 550000 / 500000 |
| 3 | 0x300000 / 0x0C0000 | 825000 / 750000 |
| 4 | 0x400000 / 0x100000 | 1100000 / 1000000 |

流程：保存原配置 → ORIGINAL 重应用及稳定 → 3轮校准并冻结尺度/死区 → ORIGINAL 重应用及稳定 → 3轮正式基线 → 每个候选3轮 → 每个候选后ORIGINAL 3轮漂移检查 → 选择有效改善者 → 最佳参数重应用、稳定及3轮复验 → DONE。无改善也复验 ORIGINAL，返回 NO_IMPROVEMENT。候选安全不合格立即恢复 ORIGINAL 再测控制基线，数据/接口故障则终止并回退。

默认每轮1000ms；最短稳定等待测频300ms、锁相800ms，连续50ms锁定，最长3000ms。总超时120秒。失败/取消恢复完整原参数并等待重新锁定；恢复失败独立报告。仅 HOST_ONLY，未实现的开机/自动恢复策略明确拒绝。主循环推进，有界原子提交。

## 2. 完整使用已有指标

每轮丢弃当前快照及其后第一份新快照，才累计完整窗口；重复不累计，跳号记录且断开符号相邻关系，序号倒退/复位否决。100ms没有新数据失败。序号按模运算支持回卷，HAL跳过0时记缺失。累计事件差帮助发现丢窗期间失锁/限幅，事件计数饱和或倒退否决。

| 指标 | 用途 |
|---|---|
| N、窗口数、序号、缺失、读取错误、实际时间覆盖 | 数据有效性；默认至少400窗、覆盖率80%，按实际经过时间算，非独立样本置信度 |
| 频率signed mean/RMS/std/MAE/peak | RMS主评分，MAE/peak辅助；mean/std解释偏差和波动，避免重复加权 |
| 相位signed mean/MAE/peak | 相位偏差和误差幅度评分；偏差考虑监视器offset；没有平方和，不伪造相位RMS |
| 相位首末斜率 | 有效窗口平均绝对斜率除时钟率后作辅助评分；失锁、相位饱和时无效 |
| 带死区翻转率 | 任务固定死区=max(1,校准RMS均值×0.1)，只比较连续有效且非中性窗口，幅度加权辅助评分 |
| lock/residual/phase saturation/rail | 默认锁定95%、越限≤5%、趋势有效≥95%；限幅和相位饱和立即淘汰 |
| 幅值mean/min/max | 均值≥16 raw，相对校准组平均幅值0.5～2倍；范围作输入诊断，不奖励信号变大 |
| output min/max | 输出边界检查，活动范围与剩余空间辅助评分 |
| 失锁/限幅事件、commit error | 安全否决、漏窗期间异常检测和诊断 |

配置值为工程默认策略，不宣称实测标定。UART空闲时可配置每轮时间、重复次数、最小覆盖率、幅值底限和最小改善比例。任务内冻结配置。空闲时成功修改配置会使旧PARAMS_VALID失效，防止把旧门限下的验证当作新门限已通过。

## 3. 可复算评分与选参

独立校准组完成后冻结 F=max(校准频率RMS均值,1)，P=max(校准相位MAE均值,abs(校准相位均值的均值+offset),1024)，D=max(校准绝对斜率均值/clock,1)。正式基线与所有候选重新采样，共用这些固定尺度和死区；不能靠事后重算分数恢复不同死区下的符号对。O为配置输出上下限跨度。

    J = freqRMS/F + .05*freqMAE/F + .05*freqPeak/F
      + .20*abs(phaseMean+offset)/P + .10*phaseMAE/P + .05*phasePeak/P
      + .10*meanAbsSlopePerSample/D
      + .05*flipRate*meanWindowAbsFrequency/F
      + .05*outputSpan/O + .05*(1-outputHeadroom)
      + 2*(1-lockFraction) + residualBadFraction

先硬门槛后评分，double比较。翻转无有效对时仅辅助项为0，valid位明确标不可用。所有已有指标都有安全/性能/诊断用途，不必每个都重复加权。

与ORIGINAL五个PID字段完全相同的候选保留测量和控制复测，但不参与新参数选优，避免将时间波动误报为调参改善。其余候选所有重复轮合格才比较均分，需改善超过 max(.02,最小相对改善×最佳均分,2×基线极差,候选极差,最佳极差)，且候选最坏轮低于最佳最好轮。默认相对改善3%，是工程迟滞而非统计置信度。ORIGINAL控制复测超出max(.1,25%基线均分,3×基线极差)则终止。最佳复验容忍max(.02,20%最佳均分,2×最佳极差,2×基线极差)，非ORIGINAL还须保留相对基线改善；复验结束时即时锁定状态也必须有效，否则回退。

## 4. UART和上位机

0x97/0x98 action0～4保持，状态版本2、18字节长度不变；旧score16展示J×1000饱和值，实际比较double。
action5请求[5,page]，page为0～8，共9页，每页44字节，包含版本/action/page/runId/reportId/类型/候选/轮次/有效位。page0冻结整份报告，后续页同一份，不拼接不同窗口。
action6读取配置；action7空闲写配置。上位机提供配置、诊断和JSONL保存；旧固件不自动请求新字段。批次默认超时150秒。

报告包含指标、否决原因、冻结归一化尺度、配置、实际参数与报告标识；原始单位raw/count/s明确展示。逐页字段和使用步骤见 [上位机说明](host_app/README.md)。每页的12字节公共头为version/action/page/runId各u8、reportId:u32LE、mode/profile/repeat/valid各u8；随后8个小端32位字段。内部评分double，遥测float32，允许浮点舍入差。

GUI逐页异步读取，只有九页标识一致且数值有限才保存完整JSONL；这是最新报告轮询，不保证保存全部轮次。PS不依赖GUI在线。配置只保存在RAM，不跨重启保存。默认完整合格流程共33轮（11组），约37～45秒加通信/调度耗时；任务截止120秒，回退另有最多3秒锁定等待。

## 5. 验证

直接测试生产C状态机：改善/无改善、重复/漏窗、符号死区、坏数据、漂移、复验劣化、取消、回退失败、旧接口、时间与序号回卷。ARM BSP编译链接、原RTL回归、Python协议/GUI回归。整机布线时序及CDC约束核对见下述记录和硬件审计；板上动态性能仍需实测。

## 6. 实施记录

- 文档先行，随后完成生产引擎、MMIO/UART适配、上位机与故障测试。
- 生产C指标/HAL、整定状态机、窗口累加和冻结报告自检通过；5项PS适配器/寄存器映射检查通过。
- 上位机46项协议、串口匹配、GUI、验证与固定PID回归通过。
- Vivado 2018.3阶段3兼容回归、5A扩展RTL随机/极值/CDC/实际窗口测试通过。
- 新增DPLL FIR同步捕获相位扫描、真实FIR IP的2184个慢域输出对照、PWM 131072组合及上电/复位逐周期等价、7地址旧实时读回/ACK回归均通过。
- VCO二级乘法流水的真实IP回归通过：40401组乘法对照、两种时钟相位各1000笔输入/输出全部完成，动态Mul/Div关联与完整输出保持固定一拍延迟。控制值到DDS新增8 ns延迟，不宣称DAC波形逐周期相同。
- ARM Cortex-A9/VFPv3 hard-float应用编译链接通过（现有BSP、libxil/libm），重现命令见 [PS测试说明](tests/ps/README.md)。
- FSBL以当前工程ps7_init.c重新构建通过；没有覆盖旧bootimage。当前PS初始化与旧平台哈希一致。
- FPGA完整布线及优化后WNS +0.065 ns、WHS +0.054 ns、WPWS 0，setup/hold/pulse-width失败终点均为0；17/12位统计窗口、真实VCO二级流水和FIR有限hold断言通过，已导出匹配bit/HDF。FIR两个首级hold余量为+0.830/+0.899 ns；最终DCP的独立单线程CDC summary/details均正常完成，报告的跨时钟终点无Unsafe/Unknown。详见 [硬件审计](tests/rtl/HARDWARE_TIMING_AUDIT_20260920.md)。
- 本地匹配启动镜像已通过Bootgen生成：`autotune_complete.sim/bootimage/BOOT_autotune.bin`，SHA256为 `AD6368F36D69E391B9F3016B4988B89CDE282D2A8C3E984C58618D86C1B636E7`。同目录manifest记录FSBL、应用ELF、bit、HDF、DCP和时序报告哈希；二进制产物留在本地，未覆盖SDK旧bootimage。
- 尚未烧写或执行板上输入阶跃、幅值变化、稳态改善及实际覆盖率验收；现有42个未指定output delay的端口仍需板级IO验收，内部时序通过不替代这些测试。
