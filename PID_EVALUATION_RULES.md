# PID 评价指标、计算分工与 Autotune 规则

> 更新：2026-09-20。本文说明当前完整 PS Autotune 的指标、计算分工和结论边界。
> 当前算法合同以 [AUTOTUNE_COMPLETE_DESIGN.md](AUTOTUNE_COMPLETE_DESIGN.md) 为准；寄存器和验收记录见 [AUTOTUNE_IMPLEMENTATION_PLAN.md](AUTOTUNE_IMPLEMENTATION_PLAN.md)。

## 1. 目标和计算分工

Autotune 在输入条件可比、闭环安全且数据可信的前提下，选择在指定稳态和动态指标上表现合适的完整 PID 参数组。锁定是合格条件，不等于参数最优。

| 层次 | 责任 | 原因 |
|---|---|---|
| PL | 同步采集残差、幅值、状态；累计和、平方和、极值与计数；形成一致快照 | 跟随环路时钟，避免 PS 逐点读取高速信号 |
| PS | 每窗计算均值/RMS/斜率；检查有效性、执行候选搜索、提交和回退 | 每窗运算量小，可脱离上位机自主运行 |
| 上位机 | 配置与启动、遥测显示、日志、复算和实验比较 | 便于分析，不作为自主决策的必要条件 |

当前 PC 计算固定 PID 实验的统计，是实验流程分工，与PS无关。

## 2. 当前实现范围

| 功能 | 当前行为 | 结论边界 |
|---|---|---|
| PS Autotune | AutotuneEngine.c 执行完整参数保存、校准、重复基线/候选、漂移检查和最佳复验；所有现有指标用于安全、评分或诊断 | 有界 Kp/Ki 候选搜索，不是对象辨识或连续全局最优求解；Kii/Kd/D_COEF继承原值 |
| PL 指标与提交 | 两回路全速统计，快照一致性读取和完整参数原子提交 | 只保留最新窗口，PS仍必须计算缺窗和时间覆盖 |
| 上位机 Autotune | 配置、发起/取消、9页诊断、已收到报告的JSONL | 不参与运行时选参，日志不保证包含每轮 |
| GUI 固定 PID 稳定度 | 独立的1秒门宽频率实验，标准差与峰峰值排序，结束恢复ORIGINAL | 不等于PS残差评价，不自动应用排名第一组 |

缺少AD030001参数接口或AD050001指标接口时新Autotune拒绝启动。旧手动操作仍可用。工程阈值和权重已实现，尚不代表板上最优标定或动态性能合格。

## 3. 信号定义和时间尺度

### 3.1 频率残差、阈值与测量频率

物理上，同一比较点的频差可写为 e_f = f_input − f_track；阈值 T_f 是允许范围，越限条件为 abs(e_f) > T_f。输入频率、频率残差和残差阈值是三个不同的量。本项目上位机的输入标称频率来自用户设置；设备测得的频率来自下述独立门宽路径，既不由残差阈值推算，也不能仅由残差等同得到。

项目 inst_frequency 是 14 位二补码数。DDC 对包裹相位作差并叠加捕获时的相位 offset 修正。阶段 5A 保留 raw count 单位，未经数据路径、比例和延迟核对不换成 Hz。

门宽测频由另一条路径输出：

    f_i = phase_sum_i × 125000000 / (gate_clocks_i × 2^33)  [Hz]

payload 前 10 字节为小端无符号相位和，后 6 字节为门宽。固定实验门宽为 125000000 个标称 125 MHz 时钟。显示六位小数不代表微赫兹准确度；触发和通信使记录间隔不严格等于 1 秒。

### 3.2 相位残差

phase_residuals 来自 PLL_loop_filters_with_saturation.vhd 的 32 位饱和相位误差累加器：积分频率误差，解锁或 gain_changed 时清零，达到 INT32_MIN/MAX 后饱和。

先检测饱和和切换清零，再解释斜率；不能照搬对包裹相位的展开算法。相位目标需独立定义；越限监视器计算的是残差加配置 offset 后的幅度，原始相位零点不一定是门限中心。

### 3.3 实际采集窗口

| 回路 | 统计时钟 | N | 窗口时长 |
|---|---:|---:|---:|
| 测频 | 125 MHz | 131072 | 1.048576 ms |
| 锁相 | 3.125 MHz | 4096 | 1.31072 ms |

模块默认 WINDOW_LOG2=12，但测频顶层覆盖为 17。每拍计数；N 是时间采样数，不是独立样本数。N 大不证明连续观察了一秒；还需检查窗口序号、实际时间和缺失窗口。

## 4. Autotune 指标定义

以下 N 为同一完整窗口的采样点数，Ts 为环路采样周期。

| 指标 | 公式/定义 | 用途 |
|---|---|---|
| 频率残差均值 | μf = Σe_f / N | 持续有符号频差 |
| 平均绝对频率残差 | Σabs(e_f) / N | 辅助误差尺度，不是 RMS |
| 频率残差 RMS | sqrt(Σe_f² / N) | 总体误差，包含偏差和波动 |
| 频率残差标准差 | sqrt(max(0, Σe_f²/N − μf²)) | 围绕窗口均值的波动，采用总体分母 N |
| 相位残差均值 | μp = Σe_phi / N | 相位位置，相对目标解释 |
| 相位残差绝对均值/峰值 | Σabs(e_phi)/N；max(abs(e_phi)) | 现有平均误差与最坏偏离 |
| 首末相位平均斜率 | (phase_last − phase_first) / ((N−1)Ts) | 窗口净漂移，不是线性回归拟合 |
| 相位饱和比例 | phase_sat_count / N | 饱和使斜率失真，需否决趋势解释 |
| 频率窗口符号翻转率 | 相邻有效窗口均值异号次数 / 可比较窗口对数 | 已接入，带死区且幅度加权的振荡辅助指标 |
| 越限比例 | count(freq_bad OR phase_bad) / N | 合格性和异常记录 |
| 限幅比例 | count(positive_rail OR negative_rail) / N | 安全合格性 |
| 锁定比例 | locked_count / N | 合格门槛，不证明最佳 |
| 样本数、窗口数、覆盖率 | 实际计数/序号与时间 | 数据完整性和可比性，不奖励更多样本 |

采用分母 N 时，RMS² = μf² + σf²。例如频差恒定 2 raw count，标准差为 0，RMS 为 2。旧绝对值路径在最小负数处采用饱和近似；5A 新平方和保留 -8192 的精确平方。

相位以弧度表示、同一比较点且尺度一致时，物理关系为 e_f = (1/2π) × d(e_phi)/dt。原始寄存器存在积分、延迟、捕获修正和定点比例，不能直接套用该换算。斜率与频差相关，不应无依据重复加权。

首末斜率会遗漏窗内先升后降的振荡。相位饱和或非全程锁定时，5A 计算器将趋势标记不可用；调用者仍须排除参数切换/清零窗口。长期漂移后续使用带时间戳的连续窗口均值另行拟合。

翻转规则：μf > +d 为正，μf < −d 为负，其余中性；只比较序号连续且双方非中性的窗口。中性或漏窗打断相邻关系。零次可比较时为不可用，不是“无振荡”。翻转率必须结合幅度与周期；单向偏差也可能零翻转。

## 5. 当前评价顺序

1. 数据有效：完整快照、样本与时间覆盖达标，无不支持字段、溢出或跨阶段混入。
2. 输入可比：幅值与输入条件足以支持比较；相对基线幅值不能替代绝对信号存在判断。
3. 合格：锁定、限幅、越限、相位饱和满足要求；不合格直接淘汰。
4. 性能：按测频/锁相目标比较残差 RMS、相位偏差和波动；斜率用于收敛，翻转率用于诊断。
5. 接受：重复评价后改善超过测量波动和门限，再替换最佳参数；同分或不可分辨时保持。
6. 验证：最佳参数重新应用、稳定后测量，验证合格性与性能复现，失败回退。

当前先用独立校准组冻结归一化尺度和死区，再重采正式基线；最小改善同时受重复轮极差、绝对底限和相对比例限制。默认3%是可配置工程迟滞，不是统计置信结论，须结合板上数据标定。相位 RMS 需要平方和或明确量化方案，5A 不从绝对值和伪造 RMS。

只比较稳定输入可能偏向窄带、恢复慢的参数；动态测试应包含受控频率变化、幅值变化后的恢复时间、峰值误差和失锁。稳态与动态结论分别给出。PID 整定需要兼顾性能与鲁棒性，参见 [MathWorks 整定目标说明](https://www.mathworks.com/help/control/getstart/pid-tuning-algorithm.html)。

## 6. 当前 PS 规则与旧日志区别

完整公式、门限、候选接受和复验规则见 [完整合同第2～3节](AUTOTUNE_COMPLETE_DESIGN.md)。默认至少400个新窗口、80%实际时间覆盖、95%锁定及趋势有效比例、最多5%残差越限；限幅和相位饱和直接否决。绝对幅值下限16 raw且相对校准幅值0.5～2倍。重复/跳号、时间/序号回卷、计数溢出和提交错误均显式处理。

频率RMS是主要误差项；相位偏差、残差幅度/峰值、斜率、带幅度翻转率、输出活动范围/余量、锁定和越限用于辅助评价。频率均值和标准差保留用于解释RMS来源，不重复加权。幅值极值、单项越限和事件数用于诊断及安全。完整使用指标不要求每个字段都作为独立得分项。

内部double比较；UART v2的18字节主状态仅显示J×1000，65535可能表示无有效评分或显示饱和，须查浮点诊断。每轮score与本轮指标对应，重复组均分/极差另列字段。候选改善必须压过噪声迟滞且最坏候选轮优于最佳组最好轮；正式基线、穿插ORIGINAL与最终复验全部使用相同冻结尺度。

旧v1日志采用整数MAE和状态罚分，曾有频率小于1、相位MAE小于1024取整为零，候选Kii/Kd为零，以及只比较一次窗口的问题。新旧评分单位与流程不同，不能横向比较数值；旧Scheme B瞬时轮询不再作为新Autotune的兼容回退路径。

## 7. GUI 固定 PID 稳定度规则（独立保留）

默认每轮 N=30 个新触发的 1 秒门宽读数，重复 R=3，写入后等待 3 秒。PID/门宽读回一致、两路自调空闲、测量有效且采样前后状态 (status & 0x3F)==0x30 才接受。

    mean = Σf_i / N
    std = sqrt(Σ(f_i−mean)² / (N−1))   # Python statistics.stdev
    p2p = max(f_i) − min(f_i)
    offset = mean − 用户标称频率

每组必须完成 R 轮：

    S = sqrt((std_1² + … + std_R²)/R)
    P = max(p2p_1, …, p2p_R)
    M = (mean_1 + … + mean_R)/R
    按 (S, P) 升序；完全相等时保持原出现顺序

S 是各轮标准差 RMS，不是频率 RMS。P 仅在 S 的完整浮点值相等时决定先后；没有加权总分或显著性门限。mean/offset/min/max 记录但不直接排序。

不拼接不同轮次，是为了不把轮间均值变化全部计入轮内波动；因此各轮稳定但均值持续漂移时 S 仍可很小。必须同时看各轮 mean，不能称其为长期稳定度排名。

例：A 三轮 std 均为 1 Hz、峰峰均为 3 Hz；B 三轮 std 均为 0.8 Hz、峰峰均为 5 Hz。当前规则 B 优先。恒定 -14 Hz 偏差可有零标准差；显示接近标称值不能证明准确度提高。

失锁、停止、超时、异常或恢复失败时不输出最终排名。成功结束恢复 ORIGINAL 和门宽，不自动应用第一名。raw.csv 中记录的值可能在随后的状态检查失败，完整一轮以 GROUP_COMPLETE 和 summary.csv 为准。

轮询不保证门宽内部从未失锁；相关样本的 N−1 修正不保证无偏或严格置信区间；该统计不是 Allan 偏差。门宽、样本数、输入条件和观察时长应一致。

## 8. 与 PID 的关系及结论边界

| 参数 | 主要改变的行为 | 应关注 |
|---|---|---|
| Kp | 比例响应、带宽和阻尼 | 波动、峰值、恢复与振荡 |
| Ki | 积分纠偏与低频跟踪 | 持续偏差、相位漂移、低频摆动 |
| Kii | 更低频积分与稳定裕量 | 更长时间漂移、慢振荡和恢复 |
| Kd/D_COEF | 微分响应与滤波 | 瞬态、噪声放大、阻尼折中 |

这是一般作用方向，不是寄存器值到物理带宽的标定。指标评价整个闭环，不存在“RMS 只对应 Kp”“斜率大必然增大 Ki”的一一对应。定点比例、延迟、采样率、限幅和输入噪声都会影响结果。

正式结论应携带实际 PID、数据源与单位、窗口、样本和时间覆盖、输入幅频、参考时钟、重复实验及恢复结果。全部零或近似并列应先查分辨率与原始数据，不能强行宣布某组最优。
