# PID Autotune 实现、寄存器与验收记录

> 更新：2026-09-20。阶段5B/5C已合并实现；现行算法以 [AUTOTUNE_COMPLETE_DESIGN.md](AUTOTUNE_COMPLETE_DESIGN.md) 为准。下文保留阶段5A寄存器合同和历史执行记录。
> 目标：PL 采集、PS 自主评价和离散选参，上位机负责配置、显示与记录。

## 1. 实施范围与依据

- 评价定义以 `PID_EVALUATION_RULES.md` 为准；通信、互斥和回退原则沿用 `ADAPTIVE_DPLL_PS_STRATEGY.md`。
- 两条回路分别标定。测频实际窗口为 2^17 / 125 MHz = 1.048576 ms；锁相为 2^12 / 3.125 MHz = 1.31072 ms。
- 每个环路时钟都被统计。N 表示时钟采样数，不表示独立测量数，也不自动证明连续观察了一秒。
- `inst_frequency` 是 14 位有符号 DDC 相位差分及捕获相位修正量；不是门宽测频的 Hz。
- `phase_residuals` 来自 32 位饱和相位误差累加器，解锁或 `gain_changed` 会清零。它不是任意可直接展开的包裹相位。饱和时零斜率不能证明无频差。
- 固定 PID 页面继续保留原排序与恢复语义。新算法先在 HOST_ONLY 模式验证；Kii/Kd/D_COEF 不新增自动搜索。

## 2. 阶段划分

| 阶段 | 内容 | 完成条件 |
|---|---|---|
| 5A | 指标定义、PL 扩展快照、CDC、PS HAL、离线指标计算 | 已完成代码与离线验证，详情见执行记录 |
| 5B | PS 评价窗口管理和诊断上报 | 已实现：覆盖、缺窗、切换隔离、死区、9页冻结诊断和JSONL |
| 5C | 新评分、重复候选比较和最终验证 | 已实现：校准/基线/候选/漂移/复验和完整回退；工程阈值待板上标定 |
| 5D | 动态验证和整机验收 | 完整综合/实现/时序/CDC、输入阶跃和幅值变化实测；报告适用范围 |

阶段5A仅提供观测基础；本次5B/5C已接入运行时选参。默认权重和阈值作为显式工程策略提供，不能声称已经板上科学标定。

## 3. 阶段 5A 数据接口合同

参数接口 `0x0060 = 0xAD030001` 和已有快照寄存器保持兼容。两条回路使用相同的 word 地址，叠加不同基地址：

- 测频回路：`0x40200000`
- 锁相回路：`0x40600000`

PS 字节地址计算为 `base + 4 × word_address`，多字寄存器按低 word、再高 word 读取，小端拼接。一次快照读取由 `0x0120` 开始，读取到 `0x0141` 结束；其中 `0x0135`、`0x0136` 不是窗口样本统计量，详见“范围/说明”列。

| Word 地址 | 寄存器 | 类型 | 范围/说明 |
|---|---|---|---|
| `0x0120` | `SNAPSHOT_SEQ` | u32 | 窗口序号；每完成一个窗口加 1，复位初值为 0 |
| `0x0121` | `SAMPLE_COUNT` | u32 | 本冻结快照包含的采样数；测频为 `2^17`，锁相为 `2^12` |
| `0x0122～0x0123` | `AMP_SUM` | u64 | 窗口幅值累加和 |
| `0x0124` | `AMP_MIN_MAX` | u16 + u16 | 低 16 bit 为幅值最小值，高 16 bit 为幅值最大值 |
| `0x0125～0x0126` | `FREQ_ABS_SUM` | u64 | 窗口频率残差绝对值累加和 |
| `0x0127` | `FREQ_ABS_MAX` | u32 | 窗口频率残差绝对值峰值 |
| `0x0128～0x0129` | `PHASE_ABS_SUM` | u64 | 窗口相位残差绝对值累加和 |
| `0x012A` | `PHASE_ABS_MAX` | u32 | 窗口相位残差绝对值峰值 |
| `0x012B` | `OUTPUT_MIN` | s32 | 窗口环路输出最小值 |
| `0x012C` | `OUTPUT_MAX` | s32 | 窗口环路输出最大值 |
| `0x012D` | `LOCKED_SAMPLES` | u32 | 窗口内锁定有效样本数 |
| `0x012E` | `POS_RAIL_SAMPLES` | u32 | 窗口内正限幅样本数 |
| `0x012F` | `NEG_RAIL_SAMPLES` | u32 | 窗口内负限幅样本数 |
| `0x0130` | `FREQ_BAD_SAMPLES` | u32 | 窗口内频率残差越限样本数 |
| `0x0131` | `PHASE_BAD_SAMPLES` | u32 | 窗口内相位残差越限样本数 |
| `0x0132` | `LOSS_LOCK_EVENTS` | u32 | 复位以来失锁下降沿累计次数的窗口快照 |
| `0x0133` | `POS_RAIL_EVENTS` | u32 | 复位以来正限幅上升沿累计次数的窗口快照 |
| `0x0134` | `NEG_RAIL_EVENTS` | u32 | 复位以来负限幅上升沿累计次数的窗口快照 |
| `0x0135` | `COMMIT_ERRORS` | u32 | 参数提交错误累计次数，不属于窗口统计 |
| `0x0136` | `METRICS_INFO` | u32 | 指标扩展能力标识；当前为 `0xAD050001`，不随窗口变化 |
| `0x0137～0x0138` | `FREQ_SIGNED_SUM` | s64 | `Σ e_f`，窗口频率残差有符号和 |
| `0x0139～0x013A` | `FREQ_SQUARE_SUM` | u64 | `Σ e_f^2`，包括 `-8192` 的精确平方 |
| `0x013B～0x013C` | `PHASE_SIGNED_SUM` | s64 | `Σ e_phi`，窗口相位残差有符号和 |
| `0x013D` | `PHASE_FIRST` | s32 | 窗口首个相位残差样本 |
| `0x013E` | `PHASE_LAST` | s32 | 窗口最后一个相位残差样本 |
| `0x013F` | `RESIDUAL_BAD_UNION` | u32 | `frequency_bad OR phase_bad` 的窗口样本数 |
| `0x0140` | `RAIL_UNION` | u32 | `positive_rail OR negative_rail` 的窗口样本数 |
| `0x0141` | `PHASE_SAT_SAMPLES` | u32 | 相位残差等于 `INT32_MIN/MAX` 的窗口样本数 |

旧 bitstream 在 `0x0136` 未实现时返回 0；PS 必须将其解释为“不支持扩展指标”，不能解释为测量值为零。

`SNAPSHOT_SEQ` 当前为 32 位自然回卷计数器：`0xFFFFFFFF` 后回到 `0x00000000`。由于 0 同时用于表示“尚无有效快照”，PS HAL 不接受序号为 0 的快照；长期运行时应通过跳号检测并记录该边界事件。

所有地址均为 word address，PS 字节地址 = base + 4 × word。原快照 672 bit，加上 352 bit 扩展后为 1024 bit；锁相 CDC 必须整体传递，不能为扩展字段另建不同步快照。

快照序号前后包住旧、新全部读操作；遇到变化重试。只在读取成功时使用数据。旧 HAL 仍可读取旧字段，新 HAL 对旧 FPGA 清零扩展存储并返回“不支持扩展”的标识。

有效参数范围限定为 1 ≤ WINDOW_LOG2 ≤ 31。在该范围内：频率平方和最大为 2^(26+31)=2^57，s64 相位和不会超过 2^62，均可表示。旧绝对值路径保留最小负数饱和到最大正数的历史行为；新有符号和、平方和不继承该近似。

PL 不计算平方根或除法。PS 计算每窗均值、RMS、方差和首末差斜率；频率指标单位为 raw count，相位斜率为 raw count/s。首末差是平均变化率，不宣称线性回归拟合或物理 Hz。

## 4. 已实现：窗口有效性和诊断

1. 在切换完成并等待稳定后开启评价。丢弃当前已冻结快照和可能跨过评价起点的窗口；不能只丢弃“上一份已完成窗口”就宣称时间完全隔离。
2. 记录首末序号、接收窗口数、缺失窗口数、样本数、实际观察时间。最新值寄存器可能漏窗；重复不累计，跳号不伪造连续窗口。
3. 比较相同评价时长及覆盖率；最小 800 样本是旧规则，不能直接作为新 PL 时间覆盖的验收标准。
4. 窗口频率符号取有符号均值：大于 +deadband 为正，小于 -deadband 为负，其余为中性。只比较序号相邻且双方均非中性的窗口；中性、漏窗和阶段变化打断相邻关系。分母是可比较窗口对数，分母为零时标记不可用。
5. 相位首末斜率遇到相位饱和、锁定不完整或切换窗口无效。微小相位变化必须结合频率指标；长期漂移另外用带时间戳的连续窗口均值分析。
6. 指标遥测与旧 18 字节状态分开扩展，保留旧主机兼容性。必须携带版本、单位、有效位和否决原因，不能依靠饱和的 16 位评分复现内部选参。

## 5. 已实现：分层评价和有限搜索

顺序为数据有效 → 输入条件可比 → 锁定/限幅/越限/相位饱和合格 → 稳态性能比较 → 最佳参数重新验证。

- 基线与候选使用相同时间、相同窗口规则和重复次数；穿插重复基线以识别输入或环境漂移。
- 新评分考虑频率残差 RMS、相位目标偏差/波动；斜率主要辅助收敛检查，翻转率主要辅助振荡诊断。相关量不重复无依据加权。
- 归一化尺度由独立校准组均值及工程底限确定并冻结。频率/相位锁定阈值不可未经核对直接代入评分；当前相位越限监视器还包含 offset。
- 通过重复基线确定可分辨改善量。只有超过绝对改善底限和相对改善门限、且重复评价支持的候选才替换；当前工程默认值与可配置范围见完整合同，板上标定仍待进行。
- 保持当前四组有界 Kp/Ki 候选作为第一轮实验输入；它们不是已证明安全或最优的标定值。先完成 profile 比较，再考虑受限局部搜索。
- 最佳参数应用后先稳定，再重新测量。验证必须兼顾合格性与性能复现，不能只判断仍锁定。
- 明确提前退出条件，取消和任何提交/读回故障恢复 ORIGINAL；输入变化导致不可比时中止，不能把最小分数当作成功。

## 6. 阶段 5D：动态与整机验收

使用明确幅度、持续时间和频段的小扰动测试恢复时间、峰值误差、越限持续时间和失锁；默认不自动向真实硬件注入未经标定的扰动。

测频稳定度与锁相性能分别报告，不以 1 秒门宽标准差替代环路相位噪声，也不由稳态最好推断阶跃响应最好。

必须保留 FPGA/固件版本、实际参数、参考时钟、输入幅频、统计窗口、死区、目标相位、数据覆盖率和原始快照。完整综合、实现、时序和 CDC 通过后再安排板上验证。

## 7. 执行记录

- 2026-09-07：5A 代码与离线验证完成：
  - 重写评价规则，修正窗口配置、原始频率单位和饱和相位累加器语义。
  - 扩展统计接入两回路；DPLL CDC 同步扩展为 1024 bit；已有寄存器地址及 UART 协议保持兼容。
  - HAL 按扩展能力读取，序号覆盖全部新旧字段；现行 PS 对新快照使用精确 OR 计数。评分其余公式和候选值未变。
  - 新增 AdaptiveMetrics.c/.h，每窗计算有符号均值、频率 RMS/标准差、相位首末斜率与比例；不支持数据返回错误，相位饱和或锁定不完整时趋势不可用。计算器尚未接入 Autotune 状态机或 GUI。
  - GCC C99 -Wall -Wextra -Werror 模拟 HAL/数值自检通过。
  - 两项生产顶层静态接口检查通过：新增寄存器映射、CDC 字段顺序及总位宽。
  - Vivado 2018.3 两顶层及相关模块语法解析通过；扩展 RTL 自检和阶段 3 回归通过。
  - 独立综合：WINDOW_LOG2=17 为 782 LUT、1906 FF、1 DSP；WINDOW_LOG2=12 为 779 LUT、1901 FF、1 DSP。均按 8 ns 约束，综合后 WNS 为 +2.625 ns；无锁存器。
  - Cortex-A9/VFPv3 hard-float 对 HAL、指标计算器和主程序编译通过；使用现有 BSP、platform.c 和 lscript.ld 完成应用链接（包含 libm）。SDK 项目已补充数学库，尚未通过 SDK GUI 刷新/构建或烧写。
  - 复现方法见 tests/ps/README.md 和 tests/rtl/README.md；本地生成物位于 autotune_stage5a.sim/，不作为固件发布件。
- 待验收：完整 FPGA 工程综合/实现/布线时序与物理 CDC；两回路上板快照、单位标定、串口负载下窗口覆盖。模块综合不能替代这些检查。
- 历史待办（截至2026-09-07）：5B、5C、5D。本次已实现5B/5C，5D验收见当前完整合同；固定PID实验继续独立运行。

## 当前完整实现（2026-09-20）

- 新增可脱离BSP测试的AutotuneEngine，生产helloworld适配器负责MMIO/UART、双回路互斥、原子提交与读回。全部指标接入，独立校准后重新采集正式基线。
- 候选继承ORIGINAL的Kii/Kd/D_COEF；坏数据终止、安全不合格候选恢复ORIGINAL、控制漂移终止、最佳复验失败回退，回退失败单独报告。
- v2保留18字节主状态，新增9页冻结报告和配置读写；GUI完成配置读回核对、诊断显示和完整报告JSONL，v1兼容。
- 测试和ARM构建入口为 `tests/ps/build_autotune.ps1`，SDK刷新源码时必须包含AutotuneEngine.c及AdaptiveMetrics.c，链接libm。不修改生成的Debug makefile。
- 最终验证与硬件交付限制统一记在 [完整合同](AUTOTUNE_COMPLETE_DESIGN.md) 及 [时序审计](tests/rtl/HARDWARE_TIMING_AUDIT_20260920.md)。旧bootimage引用2024年bitstream，禁止据此打包当前固件；必须使用本次匹配的完整工程实现输出。
