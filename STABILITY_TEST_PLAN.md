# 固定 PID 稳定度比较

默认只测试测频回路，不启动 PS Autotune。每组参数写入并读回后等待稳定，使用相同门宽连续测频，保存原始值并比较标准差和峰峰值。结果仅推荐，不自动应用。

各指标公式、评价意义、PID 关系和排序示例见 [PID_EVALUATION_RULES.md](PID_EVALUATION_RULES.md)。当前采用统计量优先级排序，没有加权总分；该文档同时说明旧 PS 整数评分，避免混用。

## 执行边界

- 新增固定 PID 稳定度页，保留旧批次验证。默认 ORIGINAL 加四组测频候选，稳定等待 3 秒，每组 30 个独立的 1 秒门宽读数，重复 3 次。中心频率由用户预先配置，D_COEF 和限幅不改。
- 当前测频 UART 没有独立关闭接口，PID 走 legacy 写入，会有切换暂态，不声称受控解锁或无扰切换。用户确认候选安全后运行；期间屏蔽其他 GUI 控制。
- 开始前检查两路自调空闲、测频器空闲，保存原 PID、中心频率和门宽；各组参数读回校验、等待稳定，采样前后检查锁定。每次触发只读取一个结果。
- 失锁、超时或停止后中止整批，尝试恢复原 PID 和门宽并读回；断连或恢复失败明确提示人工恢复，恢复参数不代表重新锁定。
- 原始 CSV、事件 JSONL、汇总 CSV 分别保存。完整组按各轮样本标准差均方根排序，峰峰值辅助比较；仅推荐，不自动应用。零方差或近似并列不声称显著改善。
- 偏差以用户填写的标称频率为参考，不代表绝对精度。状态轮询不能保证门宽内部无瞬时失锁，测得的是门宽平均频率稳定度而非全速相位噪声。

## 验收

离线覆盖统计、计划解析、采样顺序、错误与恢复；上板确认有效读数数量和恢复结果。PS 评分公式本阶段不变。
